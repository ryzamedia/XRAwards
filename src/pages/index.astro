---
export const prerender = true; // Static page for better performance

import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import KeyDates from '../components/sections/KeyDates.astro';
import CTAFooter from '../components/CTAFooter.astro';
import { supabase } from '../utils/supabase';
import { getR2ImageUrl } from '../utils/r2';
import { getCTAButton } from '../utils/date-checker';

// Fetch active event details
const { data: eventDetails } = await supabase
  .from('event_details')
  .select('*')
  .eq('is_active', true)
  .single();

// Fetch previous event details (most recent non-active event)
const { data: previousEventDetails } = await supabase
  .from('event_details')
  .select('*')
  .eq('is_active', false)
  .order('event_year', { ascending: false })
  .limit(1)
  .single();

// Fetch first 4 judges for the active event with their tags
const { data: judges } = await supabase
  .from('judges')
  .select(`
    id,
    first_name,
    last_name,
    job_title,
    organization,
    profile_image_url,
    sort_order,
    judge_events!inner(event_id),
    judge_tags(
      tags(
        id,
        name
      )
    )
  `)
  .eq('judge_events.event_id', eventDetails?.id)
  .eq('is_visible', true)
  .order('sort_order', { ascending: true })
  .limit(4);

// Fetch sponsors for the active event
const { data: sponsors } = await supabase
  .from('sponsors')
  .select(`
    id,
    name,
    logo_url,
    sort_order,
    sponsor_events!inner(event_id)
  `)
  .eq('sponsor_events.event_id', eventDetails?.id)
  .eq('is_visible', true)
  .order('sort_order', { ascending: true });

// Get dynamic CTA button based on event phase
const ctaButton = await getCTAButton('hero');

// Get hero image URL
const heroImageUrl = getR2ImageUrl('/images/2025/12/stage-no-screen-2.png');
const unitedXRLogoUrl = getR2ImageUrl('/images/2025/10/xr-awards-united-xr-logo_1760202728750.png');
const whatsNew2025ImageUrl = getR2ImageUrl('/images/2025/10/apple-holding-2024-trophy_1760204737832.jpg');
const highlights2024ImageUrl = getR2ImageUrl('/images/2025/10/obsidian-award-winner-2024_1760204866862.png');

// Testimonial logos
const logoVirtualware = getR2ImageUrl('/images/2025/10/logo-virtualware-white_1760206577461.png');
const logoCloudhead = getR2ImageUrl('/images/2025/10/logo-cloudhead-games-white_1760206625735.png');
const logoVRScout = getR2ImageUrl('/images/2025/10/logo-vrscout-white_1760206759956.png');
const logoMagnopus = getR2ImageUrl('/images/2025/10/logo-magnopus-white_1760206671197.png');
const logoGoogle = getR2ImageUrl('/images/2025/10/logo-google-white_1760206709222.png');
const logoMeta = getR2ImageUrl('/images/2025/10/logo-meta-white_1760205884886.png');

// Company logos for winners showcase
const logoApple = getR2ImageUrl('/images/2025/10/logo-apple-white_1760205864042.png');
const logoQualcomm = getR2ImageUrl('/images/2025/10/logo-qualcomm-white_1760205901474.png');

// Winner showcase images
const appleVisionProImage = getR2ImageUrl('/images/2025/10/xr-head-mounted-display-of-the-year-winner-2024_1760207686185.jpg');
const qualcommImage = getR2ImageUrl('/images/2025/10/outstanding-xr-company-of-the-year-winner-2024_1760207732668.jpg');
const asgardsWrathImage = getR2ImageUrl('/images/2025/10/xr-game-of-the-year-winner-2024_1760207777680.jpg');
---

<Layout
  title="AIXR XR Awards - Recognizing Excellence in VR, AR & MR"
  description="The AIXR XR Awards sit at the heart of recognising excellence in virtual and augmented reality. Now part of United XR, we've joined forces with AWE and Stereopsia's European XR Awards to expand our global reach."
>
  <script is:inline type="application/ld+json" slot="head">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "@id": "https://xrawards.aixr.org/#organization",
  "name": "AIXR XR Awards",
  "alternateName": "XR Awards",
  "url": "https://xrawards.aixr.org",
  "logo": {
    "@type": "ImageObject",
    "url": "https://xrawards.aixr.org/logo.svg",
    "width": "1200",
    "height": "630"
  },
  "description": "The AIXR XR Awards sit at the heart of recognising excellence in virtual and augmented reality.",
  "knowsAbout": [
    "Virtual Reality",
    "Augmented Reality",
    "Mixed Reality",
    "XR Technology",
    "Awards Ceremony"
  ]
}
  </script>

  <Header />

  <main class="min-h-screen">
    <!-- Hero Section -->
    <section class="relative min-h-screen flex items-center overflow-hidden bg-[#0E1022]">
      <!-- Background Image -->
      <div class="absolute inset-0 z-0">
        <Image 
          src={heroImageUrl} 
          alt="XR Awards Hero Background"
          class="w-full h-full object-cover"
          width={1920}
          height={1080}
          loading="eager"
        />
        <div class="absolute inset-0 bg-gradient-to-r from-[#0E1022]/90 via-[#0E1022]/70 to-transparent"></div>
      </div>
      
      <!-- Content -->
      <div class="relative z-10 max-w-6xl mx-auto px-6 sm:px-6 lg:px-8 py-32 w-full">
        <div class="max-w-5xl">
          <!-- Date and Location -->
          <p class="text-sm md:text-base text-white mb-8 font-mono">
            {eventDetails?.location || 'Location TBA'}
            <span class="text-orange-500"> | </span>
            {eventDetails?.awards_ceremony ? new Date(eventDetails.awards_ceremony).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) : eventDetails?.event_year || new Date().getFullYear()}
          </p>
          
          <!-- Main Branding -->
          <div class="mb-8 grid grid-cols-1 md:grid-cols-[auto_1fr] gap-6 md:gap-16 items-center">
            <!-- Column 1: Logo -->
            <div class="pr-0 md:pr-12">
              <Image 
                src={unitedXRLogoUrl} 
                alt="United XR" 
                class="h-16 md:h-24 lg:h-28 w-auto"
                width={400}
                height={200}
                loading="eager"
              />
            </div>
            
            <!-- Column 2: Headline -->
            <h2 class="text-xl md:text-3xl lg:text-4xl font-normal text-white leading-relaxed">
              The largest global celebration of VR & AR announces winners
            </h2>
          </div>
          
          <!-- Carousel Indicators -->
          <div class="flex items-center gap-2 mb-8">
            <button class="w-2 h-2 rounded-full bg-cyan-400"></button>
            <button class="w-2 h-2 rounded-full bg-cyan-400/40"></button>
            <button class="w-2 h-2 rounded-full bg-cyan-400/40"></button>
            <button class="w-2 h-2 rounded-full bg-cyan-400/40"></button>
            <button class="w-2 h-2 rounded-full bg-cyan-400/40"></button>
            <button class="w-2 h-2 rounded-full bg-cyan-400/40"></button>
          </div>
          
          <!-- CTA Button -->
          <div>
            <a
              href={ctaButton.href}
              class={`inline-flex items-center px-8 py-3 font-medium rounded-lg transition-colors text-base md:text-lg ${
                ctaButton.variant === 'primary' 
                  ? 'bg-orange-500 hover:bg-orange-600 text-white' 
                  : ctaButton.variant === 'secondary'
                  ? 'bg-gray-100 text-gray-900 hover:bg-gray-200'
                  : 'border border-gray-300 text-gray-700 hover:bg-gray-50'
              }`}
            >
              {ctaButton.text}
            </a>
          </div>
        </div>
      </div>
      
      <!-- Decorative orbs -->
      <div class="absolute top-1/4 right-10 w-64 h-64 bg-pink-500/20 rounded-full blur-3xl"></div>
      <div class="absolute bottom-1/4 left-10 w-64 h-64 bg-purple-500/20 rounded-full blur-3xl"></div>
    </section>

    <!-- About Section with Feature Cards -->
    <section class="py-16 md:py-24 bg-white">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Introductory Text -->
        <div class="max-w-3xl mb-12">
          <p class="text-base md:text-lg text-gray-800 mb-6 leading-relaxed">
            The AIXR XR Awards sit at the heart of recognising excellence in virtual and augmented reality. Now part of United XR, we've joined forces with AWE and Stereopsia's European XR Awards to expand our global reach. For the first time, we're combining awards into a single <strong class="font-semibold">XR Awards Gala</strong>.
          </p>
          <p class="text-base md:text-lg text-gray-800 mb-8 leading-relaxed">
            More than just a red carpet event, it's a culmination of year-round international efforts to spotlight outstanding work and connect the most influential voices across the industry.
          </p>
          <p class="text-sm md:text-base text-gray-600 mb-12">
            This is your moment. Be part of it.
          </p>
        </div>

        <!-- Feature Cards -->
        <div class="flex flex-col md:flex-row gap-8 md:gap-24 mb-4 md:mb-6 max-w-full">
          <!-- Card 1: What's new for 2025? -->
          <a href={eventDetails?.slug ? `/${eventDetails.slug}/` : '/register-interest/'} class="group relative w-[85vw] md:w-[41%]">
            <div class="aspect-video overflow-hidden rounded-lg">
              <Image 
                src={whatsNew2025ImageUrl} 
                alt={`What's new for ${eventDetails?.event_year || '2025'}?`}
                class="w-full h-full object-cover"
                width={600}
                height={340}
              />
            </div>
            <div class="feature-banner absolute bottom-8 md:bottom-12 md:left-8 md:-right-4 rounded-md p-4 md:p-6" style="left: 5vw; right: -5vw;">
              <div class="flex items-start justify-between">
                <p class="text-white text-lg md:text-xl font-medium pr-3 font-['Jura']">What's new for {eventDetails?.event_year || '2025'}?</p>
                <svg class="w-8 h-8 text-white flex-shrink-0 feature-arrow -mt-1 -mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 19L19 5M19 5H7M19 5V17" />
                </svg>
              </div>
            </div>
          </a>

          <!-- Card 2: See 2024 Highlights -->
          <a href={previousEventDetails?.slug ? `/${previousEventDetails.slug}/` : '#'} class="group relative w-[85vw] md:w-[41%]">
            <div class="aspect-video overflow-hidden rounded-lg">
              <Image 
                src={highlights2024ImageUrl} 
                alt={`See ${previousEventDetails?.event_year || '2024'} Highlights`}
                class="w-full h-full object-cover object-top"
                width={600}
                height={340}
              />
            </div>
            <div class="feature-banner absolute bottom-8 md:bottom-12 md:left-8 md:-right-4 rounded-md p-4 md:p-6" style="left: 5vw; right: -5vw;">
              <div class="flex items-start justify-between">
                <p class="text-white text-lg md:text-xl font-medium pr-3 font-['Jura']">See {previousEventDetails?.event_year || '2024'} Highlights</p>
                <svg class="w-8 h-8 text-white flex-shrink-0 feature-arrow -mt-1 -mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 19L19 5M19 5H7M19 5V17" />
                </svg>
              </div>
            </div>
          </a>
        </div>

        <!-- Sponsors -->
        {sponsors && sponsors.length > 0 && (
          <>
            <div class="text-left mb-8">
              <p class="text-sm text-gray-600 font-mono">Thank you to our Sponsors & Partners</p>
            </div>
            
            <!-- Sponsor Logos Grid -->
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4 md:gap-8 items-center">
              {sponsors.map((sponsor) => (
                <div class="opacity-70 hover:opacity-100 transition-opacity flex items-center justify-start">
                  {sponsor.logo_url ? (
                    <Image
                      src={sponsor.logo_url}
                      alt={sponsor.name}
                      class="max-h-12 w-auto object-contain"
                      width={120}
                      height={48}
                      loading="lazy"
                    />
                  ) : (
                    <div class="h-10 w-20 bg-gray-300 rounded flex items-center justify-center text-xs">
                      {sponsor.name}
                    </div>
                  )}
                </div>
              ))}
            </div>
          </>
        )}
      </div>
    </section>

    <!-- Key Dates Section -->
    {eventDetails && (
      <section class="section" style="background-color: #0E1022;">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="mb-16">
            <h2 class="text-4xl font-bold mb-4 text-white">{eventDetails.event_year} Key Dates</h2>
            <p class="text-lg text-gray-400">Mark your calendar for these important milestones in the awards journey</p>
          </div>

          {(eventDetails.nominations_open || eventDetails.nominations_close || eventDetails.finalists_announced || eventDetails.judging_period_start || eventDetails.awards_ceremony) ? (
            <KeyDates
              year={eventDetails.event_year}
              nominationsOpen={eventDetails.nominations_open}
              nominationsClose={eventDetails.nominations_close}
              finalistsAnnounced={eventDetails.finalists_announced}
              judgingPeriodStart={eventDetails.judging_period_start}
              judgingPeriodEnd={eventDetails.judging_period_end}
              awardsCeremony={eventDetails.awards_ceremony}
            />
          ) : (
            <a href="/register-interest/" class="inline-flex items-center px-6 py-3 bg-[#FF7733] text-white font-semibold rounded-lg hover:bg-[#E5691C] transition-colors">
              Register Interest
            </a>
          )}
        </div>
      </section>
    )}

    <!-- Winners Showcase Section -->
    <section class="section bg-light">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-12">
          <div class="mb-6 md:mb-0">
            <h2 class="text-4xl font-bold mb-4 text-left">Explore Award Winning Companies</h2>
            <p class="text-lg text-secondary text-left max-w-2xl">
              The impact of becoming a finalist at the XR Awards Gala
            </p>
          </div>
          <div class="flex gap-4 ml-auto">
            {previousEventDetails && (
              <a href={`/winners-and-finalists-${previousEventDetails.event_year}/`} class="inline-flex items-center px-4 py-2 bg-white text-dark text-sm font-medium rounded-lg border border-gray-300 hover:bg-gray-100 transition-colors">{previousEventDetails.event_year} Winners</a>
            )}
            {previousEventDetails?.slug && (
              <a href={`/${previousEventDetails.slug}/`} class="inline-flex items-center px-4 py-2 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary-dark transition-colors">Relive the {previousEventDetails.event_year} night</a>
            )}
          </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <!-- Left Column: 2 images stacked -->
          <div class="grid grid-rows-2 gap-6">
            <!-- Apple Vision Pro -->
            <div class="relative group overflow-hidden rounded-2xl aspect-[16/10]">
              <Image
                src={appleVisionProImage}
                alt="Apple Vision Pro"
                class="w-full h-full object-cover transition-transform duration-500 ease-out group-hover:scale-110"
                width={800}
                height={500}
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent"></div>
              <div class="absolute bottom-0 left-0 p-6 text-white">
                <div class="flex items-center gap-3 mb-6">
                  <Image 
                    src={logoApple} 
                    alt="Apple" 
                    class="h-10 w-auto"
                    width={40}
                    height={40}
                    loading="lazy"
                  />
                </div>
                <h3 class="text-xl font-bold mb-1">Apple – Apple Vision Pro</h3>
                <p class="text-sm text-white/90">XR Head-Mounted-Display of the Year</p>
              </div>
            </div>

            <!-- Qualcomm -->
            <div class="relative group overflow-hidden rounded-2xl aspect-[16/10]">
              <Image
                src={qualcommImage}
                alt="Qualcomm"
                class="w-full h-full object-cover transition-transform duration-500 ease-out group-hover:scale-110"
                width={800}
                height={500}
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent"></div>
              <div class="absolute bottom-0 left-0 p-6 text-white">
                <div class="flex items-center gap-3 mb-6">
                  <Image 
                    src={logoQualcomm} 
                    alt="Qualcomm" 
                    class="h-8 w-auto"
                    width={120}
                    height={40}
                    loading="lazy"
                  />
                </div>
                <h3 class="text-xl font-bold mb-1">Qualcomm</h3>
                <p class="text-sm text-white/90">Outstanding XR Company of the Year</p>
              </div>
            </div>
          </div>

          <!-- Right Column: 1 full height image -->
          <div class="relative group overflow-hidden rounded-2xl aspect-[16/10] lg:aspect-auto lg:h-full">
            <Image
              src={asgardsWrathImage}
              alt="Meta & Sanzaru Games – Asgard's Wrath 2"
              class="w-full h-full object-cover object-top transition-transform duration-500 ease-out group-hover:scale-110 lg:absolute lg:inset-0"
              width={800}
              height={1000}
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent"></div>
            <div class="absolute bottom-0 left-0 p-6 text-white">
              <div class="flex items-center gap-3 mb-6">
                <Image 
                  src={logoMeta} 
                  alt="Meta" 
                  class="h-5 w-auto"
                  width={80}
                  height={40}
                  loading="lazy"
                />
              </div>
              <h3 class="text-2xl font-bold mb-1">Meta & Sanzaru Games – Asgard's Wrath 2</h3>
              <p class="text-base text-white/90">XR Game of the Year</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Judges Section -->
    <section class="section relative overflow-hidden">
      <!-- Grid Pattern Background -->
      <div class="absolute inset-0 judges-grid-wrapper" style="perspective: 1000px;">
        <div class="absolute judges-grid" style="top: -150%; left: -100%; right: -100%; bottom: -100%; background-image:
          repeating-linear-gradient(0deg, transparent, transparent 58px, rgba(73, 35, 235, 0.15) 58px, rgba(73, 35, 235, 0.15) 60px),
          repeating-linear-gradient(90deg, transparent, transparent 58px, rgba(73, 35, 235, 0.15) 58px, rgba(73, 35, 235, 0.15) 60px),
          radial-gradient(circle at center, rgba(73, 35, 235, 0.3) 1px, transparent 1px);
          background-size: 60px 60px, 60px 60px, 10px 10px;
          background-position: center center;
          transform: rotateX(55deg) rotateZ(-8deg);
          transform-origin: center center;"></div>
      </div>

      <!-- Top Fade Overlay -->
      <div class="absolute top-0 left-0 right-0 h-48 bg-gradient-to-b from-[#f8f9fa] to-transparent z-10 pointer-events-none"></div>

      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="text-left mb-12">
          <h2 class="text-4xl font-bold mb-4">Over 70 Expert Judges</h2>
          <p class="text-lg text-secondary max-w-4xl mb-8">
            Our judging panel is made up of industry-leading creatives in virtual reality. We have gathered experts in various sectors of XR, from academics, journalists, and creative directors to founders and C-Suite executives.
          </p>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
          {judges && judges.map((judge) => (
            <div class="relative bg-gray-900 rounded-2xl overflow-hidden group">
              {/* Judge Image */}
              <div class="relative aspect-square overflow-hidden">
                {judge.profile_image_url ? (
                  <Image
                    src={judge.profile_image_url}
                    alt={`${judge.first_name} ${judge.last_name}`}
                    class="w-full h-full object-cover transition-transform duration-500 ease-out group-hover:scale-110"
                    width={400}
                    height={400}
                  />
                ) : (
                  <div class="w-full h-full bg-gray-700 transition-transform duration-500 ease-out group-hover:scale-110"></div>
                )}

                {/* Tag Pills - positioned at top */}
                {judge.judge_tags && judge.judge_tags.length > 0 && (
                  <div class="absolute top-3 left-3 flex flex-wrap gap-2">
                    {judge.judge_tags.map((judgeTag: any) => (
                      judgeTag.tags && (
                        <span class="px-3 py-1 bg-gray-800/90 text-white text-xs font-medium rounded-full uppercase tracking-wide">
                          {(judgeTag.tags as any).name}
                        </span>
                      )
                    ))}
                  </div>
                )}
              </div>

              {/* Judge Info - left aligned */}
              <div class="p-5 text-left">
                <h3 class="text-xl font-bold text-white mb-2">{judge.first_name} {judge.last_name}</h3>
                <p class="text-sm font-medium text-gray-300 mb-1">{judge.job_title}</p>
                <p class="text-sm text-gray-400">{judge.organization}</p>
              </div>
            </div>
          ))}
        </div>

        <div class="text-center">
          <a href="/judges/" class="inline-flex items-center px-4 py-2 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary-dark transition-colors">See the Full Judging Panel</a>
        </div>
      </div>
    </section>

    <!-- Testimonials Section -->
    <section class="relative bg-[#0E1022] py-20 md:py-32 overflow-hidden">
      <!-- Decorative Background Rings -->
      <!-- Left Ring - Offset Up with Gradient -->
      <div class="absolute -left-20 md:left-0 top-1/4 -translate-y-1/2 -translate-x-1/2 w-[300px] h-[300px] md:w-[450px] md:h-[450px]">
        <div class="absolute inset-0 rounded-full" style="background: linear-gradient(180deg, #E91E8C 0%, #5D1635 100%);"></div>
        <div class="absolute top-[55px] left-[55px] right-[55px] bottom-[55px] md:top-[75px] md:left-[75px] md:right-[75px] md:bottom-[75px] rounded-full bg-[#0E1022]"></div>
      </div>
      <!-- Right Ring - Offset Down with Gradient -->
      <div class="absolute -right-20 md:right-0 top-3/4 -translate-y-1/2 translate-x-1/2 w-[300px] h-[300px] md:w-[450px] md:h-[450px]">
        <div class="absolute inset-0 rounded-full" style="background: linear-gradient(180deg, #00D4D8 0%, #0A4B52 100%);"></div>
        <div class="absolute top-[55px] left-[55px] right-[55px] bottom-[55px] md:top-[75px] md:left-[75px] md:right-[75px] md:bottom-[75px] rounded-full bg-[#0E1022]"></div>
      </div>

      <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-left mb-8">
          <h2 class="text-4xl font-bold mb-4 text-white">What they say</h2>
          <p class="text-base text-white/80 max-w-3xl">
            Hear from previous attendees about their experience with the AIXR XR Awards and European XR Awards.
          </p>

          <!-- Decorative Dots -->
          <div class="flex gap-2 mt-6">
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
            <span class="w-1.5 h-1.5 rounded-full bg-cyan-400"></span>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-16 mt-16">
          <!-- Google Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoGoogle} 
                alt="Google" 
                class="h-10 w-auto"
                width={120}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              The AIXR XR Awards is a very important event for us and we're so excited to see all the great companies participating.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— Google</span>
            </div>
          </div>

          <!-- VR Scout Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoVRScout} 
                alt="VR Scout" 
                class="h-6 w-auto"
                width={160}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              Some of XR's biggest luminaries in attendance all there to celebrate some of the outstanding milestones in the industry.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— VR Scout</span>
            </div>
          </div>

          <!-- Meta Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoMeta} 
                alt="Meta" 
                class="h-6 w-auto"
                width={120}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              A great event that brings the XR community together to celebrate each other's success.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— Meta</span>
            </div>
          </div>

          <!-- Cloudhead Games Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoCloudhead} 
                alt="Cloudhead Games" 
                class="h-10 w-auto"
                width={180}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              The AIXR XR Awards is a fun and sophisticated event that truly connected all facets of this burgeoning industry.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— Cloudhead Games</span>
            </div>
          </div>

          <!-- Magnopus Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoMagnopus} 
                alt="Magnopus" 
                class="h-6 w-auto"
                width={160}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              Can't thank the judges, the organisers and the community enough for the passion that has gone into this event.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— Magnopus</span>
            </div>
          </div>

          <!-- Virtualware Testimonial -->
          <div class="text-left">
            <div class="mb-6">
              <Image 
                src={logoVirtualware} 
                alt="Virtualware" 
                class="h-10 w-auto"
                width={180}
                height={32}
                loading="lazy"
              />
            </div>
            <blockquote class="text-base text-white mb-6 leading-relaxed">
              The celebration of the Oscars in Virtual & Augmented reality. Unforgettable red carpet night.
            </blockquote>
            <div class="text-sm text-white/70">
              <span class="font-medium">— Virtualware</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <CTAFooter />
  </main>

  <Footer />
</Layout>

<style>
  .feature-banner {
    background: linear-gradient(135deg, #4A1B5C 0%, #3A1447 100%);
    position: relative;
    z-index: 1;
  }

  .feature-banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #5A2B6C 0%, #4A1857 100%);
    border-radius: inherit;
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }

  .group:hover .feature-banner::before {
    opacity: 1;
  }

  .feature-arrow {
    transition: transform 0.3s ease;
  }

  .group:hover .feature-arrow {
    transform: rotate(45deg);
  }

  /* Grid perspective - remove on mobile */
  @media (max-width: 768px) {
    .judges-grid-wrapper {
      perspective: none !important;
    }

    .judges-grid {
      transform: none !important;
      top: 0 !important;
      left: 0 !important;
      right: 0 !important;
      bottom: 0 !important;
    }
  }
</style>
